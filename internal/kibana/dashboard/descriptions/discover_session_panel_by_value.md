Inline Discover session configuration (`kbn-dashboard-panel-type-discover_session` config branch `0`). Set `tab` with exactly one of `dsl` (data view + query) or `esql`. The API currently restricts Discover tabs to **one** entry, so Terraform exposes a single `tab` object (not `tabs = [...]`); if Kibana raises that limit, a list-based shape could be added later without breaking existing configurations. Omit `time_range` to inherit the dashboard root time range at write time while keeping this block null in Terraform state.
