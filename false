## Task 5 Implementation Report

**Change:** fix-dashboard-schema-inconsistencies
**Schema:** spec-driven
**Top-level task completed:** 5 (Test fixtures)
**Nested subtasks completed:** 5.1, 5.2, 5.3, 5.4, 5.5, 5.6, 5.7

---

### Commits Created

1. `6937c96e` — task 5.1: update waffle acceptance test fixtures to use `config_json`
2. `1a393f42` — task 5.2: update pie chart acceptance test fixtures to use `config_json`
3. `cafceda7` — task 5.4: update treemap ES|QL acceptance test fixture to typed `esql_metrics`/`esql_group_by`
4. `f9b0e68b` — task 5.5: update mosaic ES|QL acceptance test fixture to typed `esql_metrics`/`esql_group_by`
5. `09209b6e` — task 5.6: remove required `query` from XY chart ES|QL acceptance test fixture
6. `1ce80bf7` — task 5: mark subtasks 5.1–5.7 complete in `tasks.md`

---

### Files Modified

| Subtask | Files |
|---------|-------|
| 5.1 | `internal/kibana/dashboard/testdata/TestAccResourceDashboardWaffle/basic/main.tf` |
|     | `internal/kibana/dashboard/testdata/TestAccResourceDashboardWaffle/complete/main.tf` |
| 5.2 | `internal/kibana/dashboard/testdata/TestAccResourceDashboardPieChart/basic/main.tf` |
|     | `internal/kibana/dashboard/testdata/TestAccResourceDashboardPieChart/full/main.tf` |
|     | `internal/kibana/dashboard/testdata/TestAccResourceDashboardPieChart_lensPresentationCrossCutting/cross_cutting/main.tf` |
| 5.3 | *(no-op — no fixtures contained `x_axis_json`/`y_axis_json`)* |
| 5.4 | `internal/kibana/dashboard/testdata/TestAccResourceDashboardTreemap/esql/main.tf` |
| 5.5 | `internal/kibana/dashboard/testdata/TestAccResourceDashboardMosaic/esql/main.tf` |
| 5.6 | `internal/kibana/dashboard/testdata/TestAccResourceDashboardXYChart/layers/main.tf` |
| 5.7 | *(no-op — synthetics drilldown fixtures already aligned with `urlDrilldownNestedAttributeObject` shape)* |
| —   | `openspec/changes/fix-dashboard-schema-inconsistencies/tasks.md` |

---

### Changes Summary

- **Waffle (5.1):** Renamed `metrics[].config` → `metrics[].config_json` in `basic` and `complete` fixtures.
- **Pie chart (5.2):** Renamed `metrics[].config` → `metrics[].config_json` and `group_by[].config` → `group_by[].config_json` in `basic`, `full`, and `cross_cutting` fixtures. `data_source_json` was already present in all fixtures.
- **Heatmap (5.3):** Verified that no acceptance test fixtures contained `x_axis_json` or `y_axis_json`; no edits required.
- **Treemap ES|QL (5.4):** Converted `metrics_json` raw JSON → typed `esql_metrics` block and `group_by_json` raw JSON → typed `esql_group_by` block in the `esql` fixture.
- **Mosaic ES|QL (5.5):** Converted `metrics_json` raw JSON → typed `esql_metrics` block (no `color` sub-block per mosaic schema) and `group_by_json` raw JSON → typed `esql_group_by` block in the `esql` fixture. Retained required `group_breakdown_by_json`.
- **XY chart ES|QL (5.6):** Removed the `query` block from `xy_chart_config` in the `layers` fixture, since the data layer uses an ES|QL `data_source_json` and `query` is now Optional on the schema.
- **Synthetics (5.7):** Verified that existing drilldown fixtures already use the correct shared schema shape (`url`, `label` only, no `trigger`/`type` inline attributes); no edits required.

---

### Validation Performed

- `terraform fmt` ran successfully on all modified `.tf` fixtures.
- `go build ./internal/kibana/dashboard/...` compiled cleanly with no errors.
- `make build` completed successfully (provider + docs generation).
- `make check-lint` / `make check-openspec` passed for `change/fix-dashboard-schema-inconsistencies` (4 unrelated change failures are pre-existing).

---

### Tests Run

- No acceptance tests were executed against a live Kibana instance (no `TF_ACC=1` run was performed per the "never push" and scoped-task instructions).
- Compilation validation confirms the provider and dashboard package build without errors.

---

### Blockers or Open Questions

- **None.** All 7 subtasks in task 5 are complete. The remaining acceptance test Go code (`acc_*_test.go`) still references old attribute paths (e.g., `metrics.0.config` instead of `metrics.0.config_json`) — those updates are explicitly scoped to task 6 (unit and integration tests) and were intentionally not modified in this session to stay within the assigned task-5 boundary.
