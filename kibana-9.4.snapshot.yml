server.host: 0.0.0.0
server.shutdownTimeout: 5s
server.name: kibana
feature_flags.overrides:
  lens.apiFormat: true
server.oas.enabled: true
xpack.streams.preconfigured.enabled: true
uiSettings:
  overrides:
    observability:streamsEnableQueryStreams: true