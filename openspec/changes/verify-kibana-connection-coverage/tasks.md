## 1. Provider coverage tests

- [x] 1.1 Add provider-level tests that enumerate covered `elasticstack_kibana_*` and `elasticstack_fleet_*` entities and assert `kibana_connection` presence.
- [x] 1.2 Extend those tests to assert exact shared-helper equivalence and non-deprecated schema or block metadata for covered entities.

## 2. Regression coverage

- [x] 2.1 Run the relevant provider tests to verify coverage enforcement and regression behavior.
