## Note

This change contains **no new or modified capabilities**.

It is a pure internal dependency cleanup: replacing direct imports of `github.com/hashicorp/terraform-plugin-sdk/v2` with internal equivalents and removing dead code. There are no user-facing behavioral changes, API changes, or schema changes.

Therefore, no capability specifications are required.
