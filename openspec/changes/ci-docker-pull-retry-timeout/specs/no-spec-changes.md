## No Spec Changes

This change affects CI infrastructure only (GitHub Actions workflow templates). It introduces no new Terraform provider capabilities and modifies no existing provider requirements.

No specification files are needed.
