## 1. Package rename and symbol rename

- [ ] 1.1 Move every file from `internal/resourcecore/` to `internal/entitycore/`. Rename the package declaration in `core.go`, `doc.go`, `core_test.go`, and `configure_test.go` from `package resourcecore` to `package entitycore`. Delete the now-empty `internal/resourcecore/` directory.
- [ ] 1.2 In the moved package, rename `core.go` to `resource_base.go`. Inside it, rename the type `Core` to `ResourceBase`, the constructor `New` to `NewResourceBase`, and update all internal references (including the `Configure`, `Metadata`, and `Client` receiver type). Preserve `Component`, the four `Component*` constants, and `Client()`'s signature exactly.
- [ ] 1.3 In `internal/entitycore/doc.go`, update the package comment to describe the entity core (covering resources via `ResourceBase` and data sources via `DataSourceBase`). Reuse the existing `Component` and `New` (now `NewResourceBase`) prose; preserve the documentation of the literal-suffix rule and the "Component is not a client-resolution kind" note.
- [ ] 1.4 In the moved package, rename `core_test.go` to `resource_base_test.go`. Update test names from `*Core*` to `*ResourceBase*`, update construction calls from `New(...)` to `NewResourceBase(...)`, and update assertions accordingly.
- [ ] 1.5 In `internal/entitycore/configure_test.go`, update construction calls from `New(...)` to `NewResourceBase(...)` and update any reference to the type name. Keep the existing test coverage of the diagnostics-failure rule unchanged in semantics.

## 2. New `DataSourceBase` substrate

- [x] 2.1 Add `internal/entitycore/data_source_base.go` declaring `type DataSourceBase struct { component Component; dataSourceName string; client *clients.ProviderClientFactory }`. Add `func NewDataSourceBase(component Component, dataSourceName string) *DataSourceBase`.
- [x] 2.2 Implement `(*DataSourceBase).Configure(ctx context.Context, req datasource.ConfigureRequest, resp *datasource.ConfigureResponse)` using `clients.ConvertProviderDataToFactory(req.ProviderData)`, appending diagnostics, returning early on error-level diagnostics without overwriting any prior factory, and otherwise assigning the conversion result. Behavior must match `ResourceBase.Configure` semantically.
- [x] 2.3 Implement `(*DataSourceBase).Metadata(_ context.Context, req datasource.MetadataRequest, resp *datasource.MetadataResponse)` setting `resp.TypeName = fmt.Sprintf("%s_%s_%s", req.ProviderTypeName, b.component, b.dataSourceName)`.
- [x] 2.4 Implement `(b *DataSourceBase) Client() *clients.ProviderClientFactory` with the same nil-receiver tolerance as `ResourceBase.Client()`.
- [x] 2.5 Add `internal/entitycore/data_source_base_test.go` mirroring the existing resource-base tests:
  - `Configure` stores the factory on success.
  - `Configure` does not overwrite a previously-stored factory after a diagnostics failure.
  - `Configure` accepts nil `ProviderData` (assigning a nil factory).
  - `Metadata` produces `<provider>_<component>_<data_source_name>` for representative inputs across all four components (elasticsearch, kibana, fleet, apm).
  - `Client()` returns nil on a nil receiver and on an unconfigured base.

## 3. Update all in-tree call sites

- [x] 3.1 Update every Go source file that imports `github.com/elastic/terraform-provider-elasticstack/internal/resourcecore` to import `internal/entitycore` instead. Files affected (resources): `internal/kibana/agentbuilderworkflow/resource.go`, `internal/fleet/customintegration/resource.go`, `internal/elasticsearch/ml/anomalydetectionjob/resource.go`, `internal/kibana/synthetics/monitor/resource.go`, `internal/elasticsearch/index/template/resource.go`, `internal/kibana/connectors/resource.go`, `internal/elasticsearch/security/user/resource.go`, `internal/kibana/synthetics/parameter/resource.go`, `internal/kibana/security_detection_rule/resource.go`, `internal/elasticsearch/ml/datafeed_state/resource.go`, `internal/kibana/maintenance_window/resource.go`, `internal/kibana/slo/resource.go`, `internal/elasticsearch/index/datastreamlifecycle/resource.go`, `internal/fleet/agentdownloadsource/resource.go`, `internal/kibana/agentbuilderagent/resource.go`, `internal/kibana/securityexceptionlist/resource.go`, `internal/fleet/integration/resource.go`, `internal/kibana/import_saved_objects/schema.go`, `internal/kibana/securitylistitem/resource.go`, `internal/elasticsearch/index/ilm/resource.go`, `internal/kibana/dataview/resource.go`, `internal/kibana/agentbuildertool/resource.go`, `internal/elasticsearch/inference/inferenceendpoint/resource.go`, `internal/kibana/security_exception_item/resource.go`, `internal/elasticsearch/security/systemuser/resource.go`, `internal/kibana/security_list_data_streams/resource.go`, `internal/fleet/agentpolicy/resource.go`, `internal/kibana/streams/resource.go`, `internal/fleet/output/resource.go`, `internal/elasticsearch/security/role/resource.go`, `internal/apm/agent_configuration/resource.go`, `internal/elasticsearch/security/rolemapping/resource.go`, `internal/elasticsearch/ml/datafeed/resource.go`, `internal/fleet/integration_policy/resource.go`, `internal/kibana/security_enable_rule/resource.go`, `internal/kibana/alertingrule/resource.go`, `internal/fleet/serverhost/resource.go`, `internal/elasticsearch/cluster/script/resource.go`, `internal/elasticsearch/ml/jobstate/resource.go`, `internal/fleet/elastic_defend_integration_policy/resource.go`, `internal/elasticsearch/index/index/resource.go`, `internal/kibana/synthetics/privatelocation/resource.go`, `internal/kibana/defaultdataview/resource.go`, `internal/kibana/prebuilt_rules/resource.go`, `internal/kibana/securitylist/resource.go`, `internal/elasticsearch/index/templateilmattachment/resource.go`, `internal/elasticsearch/index/alias/resource.go`, `internal/kibana/dashboard/resource.go`, `internal/elasticsearch/enrich/resource.go`, `internal/elasticsearch/watcher/watch/resource.go`, `internal/elasticsearch/security/api_key/resource.go`. Run `rg "internal/resourcecore" -l` to confirm no production files remain after the sweep (test files and contract tests are handled in tasks 3.2–3.4 and 1.4–1.5).
- [x] 3.2 In every file from task 3.1, replace `*resourcecore.Core` with `*entitycore.ResourceBase` at the embed site, and replace `resourcecore.New(resourcecore.Component<X>, "<name>")` with `entitycore.NewResourceBase(entitycore.Component<X>, "<name>")` at the constructor call. Preserve every existing `Component<X>` constant choice and every existing literal name string verbatim; this task must not change any Terraform type name.
- [x] 3.3 Rename every `resourcecore_contract_test.go` file to `entitycore_contract_test.go`. Affected files: `internal/elasticsearch/index/alias/resourcecore_contract_test.go`, `internal/kibana/dashboard/resourcecore_contract_test.go`, `internal/kibana/maintenance_window/resourcecore_contract_test.go`, `internal/fleet/customintegration/resourcecore_contract_test.go`, `internal/fleet/agentdownloadsource/resourcecore_contract_test.go`, `internal/kibana/connectors/resourcecore_contract_test.go`. Inside each file: rename the test function from `TestResource_embedsResourceCore` to `TestResource_embedsEntityCoreResourceBase`, update the import to `entitycore`, change the asserted field name from `"Core"` to `"ResourceBase"`, and change the asserted type from `reflect.TypeFor[*resourcecore.Core]()` to `reflect.TypeFor[*entitycore.ResourceBase]()`. Keep the unrelated `TestResource_importState_*` test bodies in those files unchanged.
- [x] 3.4 Update `provider/plugin_framework_resourcecore_test.go` (rename to `provider/plugin_framework_entitycore_test.go`) and any references inside it from `resourcecore` to `entitycore` and from `Core` to `ResourceBase`.

## 4. Pilot data-source migration: Elasticsearch (`enrich_policy`)

- [x] 4.1 In `internal/elasticsearch/enrich/data_source.go`, change `enrichPolicyDataSource` from `type enrichPolicyDataSource struct { client *clients.ProviderClientFactory }` to `type enrichPolicyDataSource struct { *entitycore.DataSourceBase }`.
- [x] 4.2 Replace the `NewEnrichPolicyDataSource` body with `return &enrichPolicyDataSource{DataSourceBase: entitycore.NewDataSourceBase(entitycore.ComponentElasticsearch, "enrich_policy")}`.
- [x] 4.3 Delete the package-local `Metadata` and `Configure` methods on `enrichPolicyDataSource`. Keep `Schema` and `Read` unchanged.
- [x] 4.4 Update the `Read` method to obtain the client via `d.Client()` instead of `d.client`. Verify the resulting Terraform type name is exactly `<provider>_elasticsearch_enrich_policy`.
- [x] 4.5 Add `internal/elasticsearch/enrich/entitycore_contract_test.go` containing a `TestDataSource_embedsEntityCoreDataSourceBase` that uses `reflect.TypeFor[enrichPolicyDataSource]()` and asserts an anonymous embedded field of type `*entitycore.DataSourceBase`. Mirror the structure of `internal/kibana/dashboard/resourcecore_contract_test.go` (post-rename: `entitycore_contract_test.go`).

## 5. Pilot data-source migration: Kibana (`spaces`)

- [x] 5.1 In `internal/kibana/spaces/data_source.go`, change `dataSource` from `type dataSource struct { client *clients.ProviderClientFactory }` to `type dataSource struct { *entitycore.DataSourceBase }`.
- [x] 5.2 Replace the `NewDataSource` body with `return &dataSource{DataSourceBase: entitycore.NewDataSourceBase(entitycore.ComponentKibana, "spaces")}`.
- [x] 5.3 Delete the package-local `Metadata` and `Configure` methods on `dataSource`. Keep the `var (_ datasource.DataSource ... )` interface assertions; the embedded `DataSourceBase` continues to satisfy them through promotion.
- [x] 5.4 Update every package-local site that reads `d.client` (in this file or its `Schema`/`Read` neighbors in the same package) to call `d.Client()` instead. Verify the resulting Terraform type name is exactly `<provider>_kibana_spaces`.
- [x] 5.5 Add `internal/kibana/spaces/entitycore_contract_test.go` containing a `TestDataSource_embedsEntityCoreDataSourceBase` that uses `reflect.TypeFor[dataSource]()` and asserts an anonymous embedded field of type `*entitycore.DataSourceBase`.

## 6. Pilot data-source migration: Fleet (`enrollment_tokens`)

- [x] 6.1 In `internal/fleet/enrollmenttokens/data_source.go`, change `enrollmentTokensDataSource` from `type enrollmentTokensDataSource struct { client *clients.ProviderClientFactory }` to `type enrollmentTokensDataSource struct { *entitycore.DataSourceBase }`.
- [x] 6.2 Replace the `NewDataSource` body with `return &enrollmentTokensDataSource{DataSourceBase: entitycore.NewDataSourceBase(entitycore.ComponentFleet, "enrollment_tokens")}`.
- [x] 6.3 Delete the package-local `Metadata` and `Configure` methods on `enrollmentTokensDataSource`. Keep the `var (_ datasource.DataSource ... )` interface assertions.
- [x] 6.4 Update every package-local site that reads `d.client` (in this file or its `Schema`/`Read` neighbors in the same package) to call `d.Client()` instead. Verify the resulting Terraform type name is exactly `<provider>_fleet_enrollment_tokens`.
- [x] 6.5 Add `internal/fleet/enrollmenttokens/entitycore_contract_test.go` containing a `TestDataSource_embedsEntityCoreDataSourceBase` that uses `reflect.TypeFor[enrollmentTokensDataSource]()` and asserts an anonymous embedded field of type `*entitycore.DataSourceBase`.

## 7. OpenSpec capability rename

- [ ] 7.1 At archive time, rename `openspec/specs/provider-framework-resource-core/` to `openspec/specs/provider-framework-entity-core/`. The contents of the renamed `spec.md` are replaced wholesale by the delta authored under `openspec/changes/rename-resourcecore-to-entitycore/specs/provider-framework-entity-core/spec.md`.
- [ ] 7.2 Confirm there are no remaining workspace references to the old capability name `provider-framework-resource-core` in non-archived OpenSpec artifacts (`openspec/changes/*/` excluding `openspec/changes/archive/`) after the archive step. Archived references are intentionally preserved.

## 8. Verification

- [ ] 8.1 Run `make build` and confirm a clean build with no references to `resourcecore` remaining (`rg "resourcecore" internal provider | wc -l` reports 0; `openspec/changes/archive/` is excluded from this check).
- [ ] 8.2 Run `go test ./internal/entitycore/...` and confirm all `ResourceBase` tests (renamed from `Core` tests) and all new `DataSourceBase` tests pass.
- [ ] 8.3 Run `go test ./internal/elasticsearch/enrich/... ./internal/kibana/spaces/... ./internal/fleet/enrollmenttokens/...` and confirm the new `entitycore_contract_test.go` files pass and the existing acceptance test files compile (acceptance tests themselves are not required to be re-run as part of this change since the user-observable contract is unchanged).
- [ ] 8.4 Run `go test ./...` (unit tests only) and confirm the renamed resource-side `entitycore_contract_test.go` files pass and no unit test references `resourcecore`.
- [ ] 8.5 Run `make check-openspec` (or `openspec validate` once `make setup` has installed the CLI) and confirm the change validates.
