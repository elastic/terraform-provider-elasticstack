## 1. Schema change

- [ ] 1.1 In `internal/kibana/dashboard/schema.go`, change `attrDashboardID` from `Computed / UseStateForUnknown` to `Optional + Computed / RequiresReplace`
- [ ] 1.2 Update the `MarkdownDescription` for `dashboard_id` to document: optional; when set, the resource uses PUT upsert on create; changing the value forces replacement

## 2. Create logic

- [ ] 2.1 In `internal/kibana/dashboard/create.go`, add a branch in `createDashboard()`:
  - When `planModel.DashboardID.IsKnown() && !planModel.DashboardID.IsNull()`: build the update request via `dashboardToAPIUpdateRequest` and call `kibanaoapi.UpdateDashboard` (PUT) with the plan-time `DashboardID` value
  - Extract `DashboardID` from `upsertResp.JSON201.Id` (or `JSON200.Id` if `JSON201` is nil) and build the composite `ID`
  - When `dashboard_id` is null or unknown: keep the existing `kibanaoapi.CreateDashboard` (POST) path unchanged

## 3. Model alignment

- [ ] 3.1 Verify that `dashboardToAPIUpdateRequest` produces a valid `PutDashboardsIdJSONRequestBody` from the plan model without additional changes needed for the create path
- [ ] 3.2 Ensure the model's `DashboardID` field can be set from the PUT response on create (`JSON201.Id` → `planModel.DashboardID`)

## 4. Tests

- [ ] 4.1 Unit test for `createDashboard` — mock client branches: POST path (no `dashboard_id` in plan) and PUT path (known `dashboard_id`)
- [ ] 4.2 Acceptance test `TestAccResourceKibanaDashboard_UserSuppliedID`: create a dashboard with an explicit `dashboard_id`, verify state has the supplied id, then update a non-id field and verify no replacement occurs
- [ ] 4.3 Acceptance test `TestAccResourceKibanaDashboard_AutoGeneratedID`: create a dashboard without `dashboard_id`, verify state has a Kibana-assigned UUID (existing behavior)
- [ ] 4.4 Acceptance test `TestAccResourceKibanaDashboard_IDChangeTriggersReplace`: create with an explicit `dashboard_id`, change it, verify destroy+create (replacement)
- [ ] 4.5 Confirm import round-trip for a dashboard with a human-readable `dashboard_id`: import by composite id, compare state

## 5. Spec sync

- [ ] 5.1 Run `make check-openspec`
- [ ] 5.2 Verify `make build` succeeds
