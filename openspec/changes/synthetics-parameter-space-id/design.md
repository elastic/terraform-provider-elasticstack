[{"Topic":"Pattern selection","Decision":"Use the canonical 'explicit default' space pattern (Pattern B): `kbschema.ResourceSpaceIDAttribute()` schema helper + composite `id` via `clients.CompositeID`, matching ~27 Kibana resources (tag, connectors, dataview, slo, alerting rule, ...). This is preferred over the older `KibanaUnscopedSpace` opt-out (Pattern A) used by only monitor/private-location and a few Fleet resources. Rationale: prevalent, factored (shared helper + `clients.DefaultSpaceID` + `ResolveCompositeSpaceAndID`), and internally consistent."},{"Topic":"`space_id` schema","Decision":"Define via `kbschema.ResourceSpaceIDAttribute()`: `optional, computed`, `Default: stringdefault.StaticString(clients.DefaultSpaceID)` (`\"default\"`), `UseStateForUnknown`, `RequiresReplace`. The schema default materializes `\"default\"` before create so the envelope's non-empty-space validation passes without an opt-out."},{"Topic":"Composite `id`","Decision":"Store as `<space_id>/<parameter_uuid>` via `clients.CompositeID{ClusterID: spaceID, ResourceID: uuid}.String()`. `modelFromOAPI` must accept the space from the write/read context (the API does not echo space) and assemble the composite `id`."},{"Topic":"`KibanaUnscopedSpace` removal","Decision":"Remove `IsUnscopedSpace()` and the `var _ entitycore.KibanaUnscopedSpace = Model{}` assertion. Removal is safe ONLY because the schema `Default(\"default\")` guarantees a non-empty space before create/update; the envelope then enforces non-empty space normally."},{"Topic":"`GetSpaceID()`","Decision":"Return `m.SpaceID` directly (like tag/connectors). No empty-string coercion is needed because the schema default ensures the value is known and non-empty."},{"Topic":"`GetResourceID()`","Decision":"Parse `GetID()` as composite via `synthetics.TryReadCompositeID`; return the UUID segment. Fall back to the bare `id` for legacy v0 state (bare UUID)."},{"Topic":"State migration","Decision":"NONE. No schema-version bump and no `StateUpgraders`. `resolveKibanaResourceIdentity` (`CompositeIDFromStr` + fallback) handles legacy bare-UUID `id`: a bare UUID has no `/`, falls back to `GetResourceID()`/`GetSpaceID()`, and an empty/`\"default\"` space routes to the unscoped path — correct for a legacy default-space parameter. The bare `id` is rewritten to composite form naturally on the next create/update/refresh. This matches every other Kibana resource that added `space_id`."},{"Topic":"Import","Decision":"Replace `ImportStatePassthroughID` with a custom `ImportState` built on `clients.ResolveCompositeSpaceAndID`, mirroring `internal/kibana/tag/import.go`. Bare `<uuid>` -> `space_id=\"default\"`, `id=\"default/<uuid>\"`. Composite `<space>/<uuid>` -> `space_id=<space>`, `id=<composite>`. Empty UUID segment -> error diagnostic. Update the existing `entitycore_contract_test.go::TestResource_importState_passthroughCompoundID` which currently asserts passthrough behavior."},{"Topic":"CRUD routing","Decision":"Append `kibanautil.SpaceAwarePathRequestEditor(spaceID)` to each `*WithResponse` call. `spaceID` comes from `req.SpaceID` (create/update) or the envelope-resolved `spaceID` parameter (read/delete, via `resolveKibanaResourceIdentity`)."},{"Topic":"Read populates `SpaceID`","Decision":"`modelFromOAPI` accepts a `spaceID string` argument and stores both the composite `id` and `space_id`. On plain reads the space is recovered from the composite `id` by the envelope before `readParameter` is invoked."},{"Topic":"Version gate","Decision":"No additional Kibana version floor above the existing 8.12.0 resource gate for non-default `space_id`. Kibana v8.12.0 `docs/api/synthetics/params/add-param.asciidoc` documents both `POST /api/synthetics/params` and `POST /s/<space_id>/api/synthetics/params`; public API commit `8bbb58f19aadb34f6a94bf9d77b16bc61a73091c` is an ancestor of v8.12.0. Do not add `GetVersionRequirements` for space-prefixed Parameters paths (contrast `synthetics/privatelocation`, which gates non-default space at 9.4.0)."}]
