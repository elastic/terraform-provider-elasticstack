## 1. Configured monitor visibility

- [x] 1.1 Deliver configured monitor visibility end to end: users can omit, empty, populate, or wildcard `kibana_spaces`; create and update preserve omitted versus empty API values and use `space_id` as the owning route; verify resource schema, generated API bindings, request mapping, and generated resource documentation.

## 2. Stable visibility state

- [x] 2.1 Deliver stable visibility state by retaining omitted configuration and suppressing only Kibana's automatic owning-space inclusion, while reflecting all other external visibility changes; verify focused state-mapping tests.
